<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        <then value="a"/>
    </chain>

    <chain name="chain2">
        <then value="b"/>
    </chain>

    <chain name="chain3">
        <then value="c"/>
    </chain>

    <chain name="chain4">
        <then value="a,d,c"/>
    </chain>

    <chain name="chain5">
        <then value="a,e,c"/>
    </chain>

    <chain name="chain6">
        <then value="f(d | c | b)" />
    </chain>

    <chain name="chain7">
        <then value="g,h"/>
    </chain>
</flow>