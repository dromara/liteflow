<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(a, b);
    </chain>

    <chain name="chain2">
        THEN(c);
    </chain>

    <chain name="chain3">
        THEN(d);
    </chain>

    <chain name="chain4">
        THEN(e);
    </chain>
</flow>