<?xml version="1.0" encoding="UTF-8"?>
<flow>

    <chain name="chain1">
        THEN(a, chain2);
    </chain>

    <chain name="chain2">
        THEN(b, chain3);
    </chain>

    <chain name="chain3">
        THEN(c, chain1);
    </chain>

</flow>