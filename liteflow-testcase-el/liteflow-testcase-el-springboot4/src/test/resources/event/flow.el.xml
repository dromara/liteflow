<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(a, b, c);
    </chain>

    <chain name="chain2">
        THEN(a, b, d);
    </chain>

    <chain name="chain3">
        THEN(a, e, b);
    </chain>
</flow>