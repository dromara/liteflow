<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC "liteflow" "liteflow.dtd">
<flow>
    <chain name="realOrderChain">
        THEN(realOrderAgent);
    </chain>

    <chain name="realBeanToolChain">
        THEN(realBeanToolAgent);
    </chain>

    <chain name="realFileToolsChain">
        THEN(realFileToolsAgent);
    </chain>

    <chain name="realShellToolChain">
        THEN(realShellToolAgent);
    </chain>
</flow>
