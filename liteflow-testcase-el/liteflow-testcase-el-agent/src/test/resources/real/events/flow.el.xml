<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC "liteflow" "liteflow.dtd">
<flow>
    <chain name="realEventsChain">
        THEN(realEventsAgent);
    </chain>

    <chain name="realEventsTolerantChain">
        THEN(realEventsTolerantAgent);
    </chain>

    <chain name="realEventsStrictChain">
        THEN(realEventsStrictAgent);
    </chain>

    <chain name="realEventsToolChain">
        THEN(realEventsToolAgent);
    </chain>
</flow>
