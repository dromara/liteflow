<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC "liteflow" "liteflow.dtd">
<flow>
    <chain name="realSecretChain">
        THEN(realSecretAgent);
    </chain>

    <chain name="realFixedCidChain">
        THEN(realFixedCidAgent);
    </chain>

    <chain name="realIsoAChain">
        THEN(realIsolatedAgentA);
    </chain>

    <chain name="realIsoBChain">
        THEN(realIsolatedAgentB);
    </chain>
</flow>
