<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC "liteflow" "liteflow.dtd">
<flow>
    <chain name="realFallbackChain">
        THEN(realFallbackAgent);
    </chain>

    <chain name="realRoutingChain">
        THEN(realRoutingAgent);
    </chain>

    <chain name="realTimeoutChain">
        THEN(realTimeoutAgent);
    </chain>

    <chain name="realGuardChain">
        THEN(realGuardAgent);
    </chain>
</flow>
