<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="stateAlphaChain">THEN(stateAlphaAgent);</chain>
    <chain name="stateBetaChain">THEN(stateBetaAgent);</chain>
    <chain name="stateFailureChain">THEN(failingStateAgent);</chain>
</flow>
