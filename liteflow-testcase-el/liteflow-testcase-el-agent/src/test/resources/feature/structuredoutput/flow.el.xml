<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC "liteflow" "liteflow.dtd">
<flow>
    <chain name="structuredPojoChain">THEN(structuredPojoAgent);</chain>
    <chain name="structuredSchemaChain">THEN(structuredSchemaAgent);</chain>
    <chain name="structuredEmptyChain">
        THEN(seedStructuredResponse, structuredEmptyAgent);
    </chain>
</flow>
