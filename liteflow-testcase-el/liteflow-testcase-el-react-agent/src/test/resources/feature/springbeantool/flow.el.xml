<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC "liteflow" "liteflow.dtd">
<flow>
    <chain name="componentToolChain">
        THEN(componentToolAgent);
    </chain>
    <chain name="skillToolChain">
        THEN(skillToolAgent);
    </chain>
</flow>
