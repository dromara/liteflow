<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        WHEN(sub1, sub2, sub3, sub4);
    </chain>

    <chain name="sub1">
        THEN(a);
    </chain>

    <chain name="sub2">
        THEN(b);
    </chain>

    <chain name="sub3">
        THEN(c);
    </chain>

    <chain name="sub4">
        THEN(d);
    </chain>
</flow>