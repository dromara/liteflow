<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC  "liteflow" "liteflow.dtd">
<flow>
    <chain name="chain1">
        ITERATOR(it).DO(a);
    </chain>

    <chain name="chain2">
        ITERATOR(it).DO(a).BREAK(b);
    </chain>

    <chain name="chain3">
        ITERATOR(x1).DO(
            THEN(
                c,
                ITERATOR(x2).DO(c)
            )

        );
    </chain>
</flow>