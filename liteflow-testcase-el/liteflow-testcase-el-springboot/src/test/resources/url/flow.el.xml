<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        oracleUrl = "jdbc:oracle:thin:@//localhost:1521/USERS";
        // 单行注释
        THEN(
            // 单行注释
            a.data(oracleUrl),
            b,
            WHEN(
                /**
                * 多行注释
                **/
                c,
                b
            )
        );
    </chain>
</flow>