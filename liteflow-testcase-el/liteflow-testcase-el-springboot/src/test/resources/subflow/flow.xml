<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC  "liteflow" "liteflow.dtd">
<flow>
    <chain name="chain1">
        sub = WHEN(c,d);
        THEN(a,sub,b);
    </chain>

    <chain name="chain2">
        THEN(a,subChain,b);
    </chain>

    <chain name="subChain">
        WHEN(c,d);
    </chain>

    <chain name="chain3">
        THEN(a,e);
    </chain>

    <chain name="chain4">
        WHEN(a, f);
    </chain>

    <chain name="chain5">
        FOR(100).DO(THEN(h.data("out"), g));
    </chain>

    <chain name="chain6">
        THEN(a, h.data("inner"));
    </chain>
</flow>