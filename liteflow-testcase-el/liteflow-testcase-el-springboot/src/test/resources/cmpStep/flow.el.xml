<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(a, b, WHEN(c, d));
    </chain>

    <chain name="chain2">
        THEN(WHEN(e, a).any(true), b);
    </chain>

    <chain name="chain3">
        THEN(a, b, a, a, b);
    </chain>
</flow>