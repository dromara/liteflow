flow:
  chain:
    - id: implB
      extends: base2
      value: "{3}=THEN(a,b);\n {4}=j;"
    - id: implC
      extends: base2
      value: "{3}=THEN(a,b);\n {4}=THEN(a,b).id(\"j\");"
    - id: base
      value: "THEN(a, b, {0}, {1});"
    - id: implA
      extends: base
      value: "{0}=IF(c, d, e);\n  {1}=SWITCH(f).to(j,k);"
    - id: base2
      extends: base
      value: "{0}=THEN(a,b,{3});\n {1}=SWITCH(f).to({4},k);"
