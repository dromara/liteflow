<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN( a, b, WHEN(c, d).ignoreError(true), CATCH(e), f );
    </chain>

    <chain name="chain2">
        THEN( a, b, WHEN(c, d) );
    </chain>

    <chain name="chain3">
        THEN( a, e, f );
    </chain>
</flow>