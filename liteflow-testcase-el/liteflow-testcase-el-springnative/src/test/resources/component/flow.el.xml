<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(a);
    </chain>

    <chain name="chain2">
        THEN(b);
    </chain>

    <chain name="chain3">
        THEN(c);
    </chain>

    <chain name="chain4">
        THEN(a, d, c);
    </chain>

    <chain name="chain5">
        THEN(a, e, c);
    </chain>

    <chain name="chain6">
        SWITCH(f).to(d, c, b);
    </chain>

    <chain name="chain7">
        THEN(g, h);
    </chain>
</flow>