<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(a,b,c,d,e);
    </chain>

    <chain name="chain2">
        THEN(a,b,c,WHEN(d,e));
    </chain>

    <chain name="chain3">
        THEN(a,b,c,f);
    </chain>
</flow>